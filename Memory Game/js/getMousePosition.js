//initialise
let canvas = document.getElementById('canvas');
let context = canvas.getContext('2d');
//mouseDown
canvas.addEventListener('mousedown', function (evt) {
    let canvas_x = evt.pageX;
    let canvas_y = evt.pageY;
    //alert("x: "+canvas_x+" Y: "+canvas_y)
});